---
phase: 08-content-pricing-and-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evidence-gate.github.io/index.html
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "Visitor landing on the Hero section reads copy and sees a code example that explicitly references NemoClaw blueprint validation"
    - "Visitor viewing the Pricing table sees updated feature entries reflecting v2.0 NemoClaw integration capabilities"
    - "Sharing the site URL on social platforms renders an OGP card that mentions NemoClaw and AI agent governance"
    - "Page meta description references NemoClaw integration alongside the existing Blind Gates messaging"
    - "Structured data description includes NemoClaw integration"
  artifacts:
    - path: "evidence-gate.github.io/index.html"
      provides: "Updated Hero, Pricing, and SEO/OGP content"
      contains: "nemoclaw_blueprint"
  key_links:
    - from: "og:description meta tag"
      to: "meta name=description"
      via: "consistent messaging"
      pattern: "NemoClaw"
    - from: "Hero code example"
      to: "Pricing NemoClaw gates row"
      via: "narrative consistency"
      pattern: "blueprint"
---

<objective>
Update the Hero section, Pricing table, and SEO/OGP metadata on evidence-gate.dev to reflect NemoClaw integration — the full-stack AI agent governance story.

Purpose: The current site Hero focuses only on Blind Gates. Pricing lists NemoClaw gates but doesn't highlight v2.0 capabilities. SEO meta doesn't mention NemoClaw at all. This plan brings all three in line with the complete governance narrative established in Phase 5.

Output: Updated index.html with NemoClaw-aware Hero copy/code, expanded Pricing features, and refreshed meta/OGP tags.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-architecture-diagrams-and-technical-foundation/05-01-SUMMARY.md
@.planning/phases/05-architecture-diagrams-and-technical-foundation/05-02-SUMMARY.md

<interfaces>
<!-- Current HTML regions to modify. Line numbers are approximate — use content matching. -->

From evidence-gate.github.io/index.html (SEO/OGP, lines ~8-42):
```html
<meta property="og:title" content="Evidence Gate — Quality Gates for CI/CD Pipelines">
<meta property="og:description" content="Fail-closed quality gates for AI-driven CI/CD. When AI agents write code and tests, they game visible thresholds. 29 gate types with Blind Gates that hide criteria so AI cannot optimize against them. Free for open-source.">
<meta name="description" content="Fail-closed quality gates for AI-driven CI/CD. When AI agents write code and tests, they game visible thresholds. 29 gate types with Blind Gates that hide criteria so AI cannot optimize against them. Free for open-source.">
<script type="application/ld+json">
{
  "@type": "SoftwareApplication",
  "name": "Evidence Gate",
  "description": "Fail-closed quality gates for CI/CD pipelines with tamper-proof evidence chains",
  "applicationCategory": "DeveloperApplication"
}
</script>
```

From evidence-gate.github.io/index.html (Hero, lines ~1007-1034):
```html
<section class="hero">
  <div class="container">
    <div class="hero-content">
      <p style="...">GitHub Actions · 29 Gate Types · Fail-Closed by Default</p>
      <h1>Quality Gates That <span>AI Can't Game</span></h1>
      <p>AI agents now write your code AND your tests — ... Evidence Gate hides the criteria entirely with Blind Gates, making metric gaming structurally impossible.</p>
      <div class="hero-buttons">...</div>
    </div>
    <div class="hero-code">
      <div class="hero-code-header">
        <span class="hero-code-label">.github/workflows/ci.yml</span>
      </div>
      <pre>
# Add quality gates in 3 lines
- uses: evidence-gate/evidence-gate-action@v1
  with:
    gate_type: "test_coverage"
    phase_id: "testing"
    evidence_files: "coverage.json"
      </pre>
    </div>
  </div>
</section>
```

From evidence-gate.github.io/index.html (Pricing, lines ~1439-1610):
Current rows include: 29 gate types, SARIF output, NemoClaw gates (blueprint + policy validation), AI agent repair contract (retry_prompt output), etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SEO meta, OGP tags, and structured data with NemoClaw messaging</name>
  <files>evidence-gate.github.io/index.html</files>
  <action>
Update lines ~8-42 of index.html:

1. **og:title** (line ~9): Change to:
   `Evidence Gate — AI Agent Quality Gates + NemoClaw Governance`

2. **og:description** (line ~10): Change to:
   `Fail-closed quality gates for AI-driven CI/CD with full-stack NemoClaw governance. 29 gate types including Blind Gates that hide criteria from AI agents, plus blueprint and policy validation for NemoClaw sandboxes. Free for open-source.`

3. **twitter:title** (line ~14): Same as og:title above.

4. **twitter:description** (line ~15): Same as og:description above.

5. **meta name="description"** (line ~18): Same as og:description above (keep all three descriptions identical for consistency).

6. **title tag** (line ~6): Change to:
   `Evidence Gate — AI Agent Quality Gates + NemoClaw Governance`

7. **Structured data description** (line ~30): Change to:
   `Fail-closed quality gates for CI/CD pipelines with NemoClaw sandbox governance — blueprint validation, policy enforcement, and tamper-proof evidence chains`

8. **applicationCategory** (line ~32): Keep as `DeveloperApplication` — this is correct.

IMPORTANT: Keep all other meta tags (canonical, hreflang, favicon) unchanged. The existing OGP and SEO tags reference only "Blind Gates" — the update adds NemoClaw without removing the Blind Gates messaging.
  </action>
  <verify>
    <automated>grep -c "NemoClaw" evidence-gate.github.io/index.html | head -1 && grep "og:description" evidence-gate.github.io/index.html | grep -q "NemoClaw" && echo "OGP OK" && grep "meta name=\"description\"" evidence-gate.github.io/index.html | grep -q "NemoClaw" && echo "META OK" && grep "applicationCategory" evidence-gate.github.io/index.html | grep -q "DeveloperApplication" && echo "CATEGORY OK"</automated>
  </verify>
  <done>
    - og:title, og:description, twitter:title, twitter:description, meta description, and page title all reference NemoClaw
    - Structured data description mentions NemoClaw sandbox governance
    - applicationCategory remains DeveloperApplication
    - hreflang, canonical, and favicon tags unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Hero section copy and code example for NemoClaw integration</name>
  <files>evidence-gate.github.io/index.html</files>
  <action>
Update the Hero section (lines ~1007-1034):

1. **Eyebrow text** (the green uppercase `<p>` on line ~1011): Change to:
   `GitHub Actions &middot; 29 Gate Types &middot; NemoClaw Governance &middot; Fail-Closed`

2. **Headline h1** (line ~1012): Change to:
   `Quality Gates for <span>Full-Stack AI Governance</span>`
   Rationale: Broadens from "AI Can't Game" (Blind Gates only) to the full governance story. The Blind Gates detail stays in the body copy.

3. **Body copy paragraph** (line ~1013): Replace with:
   `AI agents write your code, your tests, and now operate your infrastructure through NemoClaw sandboxes. Evidence Gate enforces quality at every layer &mdash; Blind Gates that hide CI criteria from gaming, plus blueprint and policy validation that ensures NemoClaw sandboxes deploy with correct isolation, budgets, and inference routing. Full-stack governance, fail-closed by default.`

4. **Hero code example** (lines ~1019-1031): Replace the entire `.hero-code` div content. Keep the code-header with dots but change:
   - The code label from `.github/workflows/ci.yml` to `.github/workflows/ci.yml`  (same label, still a workflow file)
   - The code block to show NemoClaw blueprint validation instead of generic test_coverage:

```
<pre><span class="code-comment"># Validate NemoClaw blueprint before deploy</span>
<span class="code-key">- uses:</span> <span class="code-string">evidence-gate/evidence-gate-action@v1</span>
  <span class="code-key">with:</span>
    <span class="code-key">gate_type:</span> <span class="code-string">"nemoclaw_blueprint"</span>
    <span class="code-key">phase_id:</span> <span class="code-string">"deploy"</span>
    <span class="code-key">evidence_files:</span> <span class="code-string">"blueprint.yaml"</span></pre>
```

IMPORTANT: Keep the hero-buttons div unchanged (Get Started Free + View Quick Start links). Keep all existing CSS classes. The hero structure (hero-content + hero-code side-by-side) does not change. Maintain the AI agent threat model narrative (LLMs gaming metrics) but extend it to include NemoClaw governance.
  </action>
  <verify>
    <automated>grep "Full-Stack AI Governance" evidence-gate.github.io/index.html | head -1 && grep "nemoclaw_blueprint" evidence-gate.github.io/index.html | head -1 && grep "NemoClaw Governance" evidence-gate.github.io/index.html | grep -q "middot" && echo "EYEBROW OK" && grep "NemoClaw sandboxes" evidence-gate.github.io/index.html | head -1 && echo "HERO OK"</automated>
  </verify>
  <done>
    - Headline reads "Quality Gates for Full-Stack AI Governance"
    - Eyebrow includes "NemoClaw Governance"
    - Body copy mentions both Blind Gates and NemoClaw sandbox governance
    - Code example shows nemoclaw_blueprint gate type with blueprint.yaml evidence file
    - Hero buttons unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Pricing table with v2.0 NemoClaw integration features</name>
  <files>evidence-gate.github.io/index.html</files>
  <action>
Update the Pricing table (lines ~1439-1610):

1. **Expand the existing "NemoClaw gates (blueprint + policy validation)" row** (line ~1521): Change the feature name to:
   `NemoClaw gates (blueprint + policy + sandbox lifecycle)`
   This reflects the full v2.0 scope — not just blueprint/policy but also sandbox lifecycle validation. Keep the same check marks across all three tiers (Free/Pro/Enterprise).

2. **Add a new row after the NemoClaw gates row** for inference routing validation:
   ```html
   <tr>
     <td>Inference routing validation (NVIDIA Cloud, NIM, vLLM)</td>
     <td class="check">&#10003;</td>
     <td class="featured check">&#10003;</td>
     <td class="check">&#10003;</td>
   </tr>
   ```
   This reflects ARCH-03 — inference routing is now a validated part of the Evidence Gate + NemoClaw story. Available in all tiers.

3. **Add a new row after inference routing** for sandbox security posture:
   ```html
   <tr>
     <td>Sandbox security posture checks (Landlock, seccomp, netns)</td>
     <td class="dash">&mdash;</td>
     <td class="featured check">&#10003;</td>
     <td class="check">&#10003;</td>
   </tr>
   ```
   Security posture checks are a Pro/Enterprise feature — deeper validation that sandbox isolation layers are properly configured. Not available in Free tier.

4. **Update the "All 29 gate types" row** (line ~1467): Change to:
   `All 32 gate types`
   (29 original + nemoclaw_blueprint + nemoclaw_policy + nemoclaw_sandbox = 32 gate types)

5. **Keep all other rows unchanged.** Do not modify: evaluations/month, API calls, SARIF, GitHub Check Runs, SHA-256, fail-closed, enforcement modes, config file, SBOM, provenance, signal-sorted summary, AI agent repair contract, presets, sticky PR, blind gate, evidence chain, quality state, remediation, missing evidence, self-hosted, custom API URL, dedicated support, or CTA buttons.

IMPORTANT: Maintain the existing table structure (thead/tbody/tfoot). Maintain the `class="featured"` on Pro column cells. New rows should be inserted in the tbody, grouped with the other NemoClaw-related row.
  </action>
  <verify>
    <automated>grep "32 gate types" evidence-gate.github.io/index.html | head -1 && grep "Inference routing validation" evidence-gate.github.io/index.html | head -1 && grep "Sandbox security posture" evidence-gate.github.io/index.html | head -1 && grep "sandbox lifecycle" evidence-gate.github.io/index.html | head -1 && echo "PRICING OK"</automated>
  </verify>
  <done>
    - "All 29 gate types" updated to "All 32 gate types"
    - NemoClaw gates row expanded to include sandbox lifecycle
    - New "Inference routing validation" row added (all tiers)
    - New "Sandbox security posture checks" row added (Pro/Enterprise only)
    - All existing rows unchanged
    - Table structure and CTA buttons intact
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, verify end-to-end:

1. HTML validity: The file should not have broken tags or unclosed elements
2. NemoClaw appears in SEO meta, Hero copy, Hero code, and Pricing table — consistent narrative
3. The AI agent threat model (LLMs gaming CI) remains in the Hero body copy alongside NemoClaw governance
4. Pricing table still renders correctly with proper column alignment (check/dash marks)
5. No content was accidentally removed from other sections (Architecture, Blind Gates, AI Governance, Ecosystem, Quick Start)
</verification>

<success_criteria>
- og:description and meta description mention both Blind Gates and NemoClaw governance
- Hero headline references full-stack AI governance
- Hero code example shows nemoclaw_blueprint gate type
- Pricing table lists 32 gate types with expanded NemoClaw features
- All content maintains the AI agent threat model narrative while adding governance layer
- File is valid HTML with no broken structure
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-pricing-and-seo/08-01-SUMMARY.md`
</output>
