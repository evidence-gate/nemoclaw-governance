---
phase: 05-architecture-diagrams-and-technical-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evidence-gate.github.io/index.html
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "Visitor can see a new 'NemoClaw Architecture' section on the site between AI Governance and Ecosystem"
    - "Visitor can see an SVG diagram that visually depicts Plugin and Blueprint as separate components with their composition relationship"
    - "The diagram labels the Plugin as 'TypeScript package extending OpenClaw CLI' and the Blueprint as 'Versioned Python artifact'"
    - "Visitor can navigate to the new section via a nav link in the top navigation bar"
  artifacts:
    - path: "evidence-gate.github.io/index.html"
      provides: "New NemoClaw Architecture section with SVG diagram, CSS, and nav link"
      contains: "id=\"nemoclaw-architecture\""
  key_links:
    - from: "nav link"
      to: "section#nemoclaw-architecture"
      via: "anchor href"
      pattern: "href=\"#nemoclaw-architecture\""
---

<objective>
Add a new "NemoClaw Architecture" section to the EN site with an SVG architecture diagram showing the Plugin+Blueprint composition, fulfilling ARCH-01.

Purpose: Establish the visual and structural foundation for NemoClaw's official architecture on the site. The SVG diagram is the centerpiece — it must accurately depict the Plugin+Blueprint relationship where the Plugin resolves, verifies, and executes the Blueprint as a subprocess.

Output: Updated index.html with new section (CSS + HTML + inline SVG + nav link)
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@evidence-gate.github.io/index.html

<interfaces>
<!-- Existing site patterns the executor must follow -->

CSS class conventions:
- Sections use `.section` and optionally `.section-alt` (alternating bg)
- Section titles: `.section-title` (h2, centered, 2rem, 700 weight)
- Section subtitles: `.section-subtitle` (centered, #64748b, 1.125rem)
- Container: `.container` (max-width 1120px, auto margins, 1.5rem padding)
- Cards: `.step-card` pattern with `.step-icon` for icon+title+text cards

Color palette:
- Primary green: #22c55e (brand), hover: #16a34a
- Text dark: #1e293b, text mid: #475569, text light: #64748b, text muted: #94a3b8
- Blue accent: #2563eb, Purple accent: #7c3aed
- Borders: #e2e8f0, Alt background: #f8fafc
- Badge green bg: #f0fdf4, badge green border: #bbf7d0

SVG pattern (from Blind Gates section):
- Inline SVG with viewBox, xmlns, role="img", aria-label
- Text elements use fill for color, font-size in px, text-anchor="middle"
- Rectangles with rx for rounding, fill + stroke
- Uses <defs> for markers
- Color coding: green for positive/good, red for problems, gray for neutral

Section insertion point: Between line 988 (end of AI Governance </section>) and line 990 (<!-- Ecosystem Section -->)

Navigation: Nav links at lines 781-789, add new link between "AI Governance" and "Ecosystem"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS for the NemoClaw Architecture section</name>
  <files>evidence-gate.github.io/index.html</files>
  <action>
Add new CSS rules in the existing `<style>` block, BEFORE the Ecosystem Section CSS comment (insert before line ~504 "/* Ecosystem Section */"). Add the following CSS classes:

1. `.arch-section` — wrapper for the architecture section content area
2. `.arch-diagram-container` — centers the SVG diagram, max-width 900px, auto margins, margin-bottom 2rem
3. `.arch-diagram-container svg` — width 100%, height auto, for responsive SVG
4. `.arch-legend` — horizontal flex row below the diagram for a small legend/caption. Font-size 0.8125rem, color #64748b, center-aligned, gap 1.5rem, justify-content center, flex-wrap wrap
5. `.arch-legend-item` — individual legend entry, flex row, gap 0.375rem, align-items center
6. `.arch-legend-dot` — 10px x 10px circle (border-radius 50%) for color indicators

Add responsive rule in the existing `@media (max-width: 768px)` block:
- `.arch-legend` should be flex-direction column, align-items center

The new section should use `.section` (NOT `.section-alt`) since AI Governance above it uses `.section` (no alt bg) and Ecosystem below uses `.section section-alt`. To maintain visual separation, this new section should use `.section-alt` so the alternation pattern continues: AI Governance (white) -> NemoClaw Architecture (gray/alt) -> Ecosystem (gray/alt is current — but wait, Ecosystem already uses section-alt).

Correction on alternating pattern: Looking at the actual sequence:
- Blind Gates: `.section` (white)
- AI Governance: `.section` (white)
- Ecosystem: `.section section-alt` (gray)

So the new section inserted between AI Governance and Ecosystem should use `.section section-alt` to create: AI Governance (white) -> NemoClaw Architecture (gray) -> Ecosystem (gray). Actually, to maintain proper alternation, the new section should be `.section section-alt` and Ecosystem should remain `.section section-alt` — two gray sections in a row is acceptable since they have different content, OR we could make Ecosystem `.section` (white) to alternate. The simpler approach: make the new section `.section section-alt` and leave Ecosystem as-is. Two gray sections in sequence is fine — the content is visually distinct.
  </action>
  <verify>
    <automated>grep -c "arch-diagram-container\|arch-legend\|arch-legend-item\|arch-legend-dot" evidence-gate.github.io/index.html | grep -q "[4-9]" && echo "PASS: CSS classes found" || echo "FAIL: Missing CSS classes"</automated>
  </verify>
  <done>All 6 CSS classes (.arch-section, .arch-diagram-container, .arch-diagram-container svg, .arch-legend, .arch-legend-item, .arch-legend-dot) present in the style block, plus responsive rule for .arch-legend</done>
</task>

<task type="auto">
  <name>Task 2: Create NemoClaw Architecture section with SVG diagram and nav link</name>
  <files>evidence-gate.github.io/index.html</files>
  <action>
**Part A — Nav link:** In the nav `<ul class="nav-links">` (around line 781-789), add a new `<li>` between the "AI Governance" link and the "Ecosystem" link:
```html
<li><a href="#nemoclaw-architecture">Architecture</a></li>
```

**Part B — New section:** Insert a new section between the closing `</section>` of AI Governance (after line 988) and the `<!-- Ecosystem Section -->` comment (line 990). The section structure:

```html
<!-- NemoClaw Architecture Section -->
<section id="nemoclaw-architecture" class="section section-alt">
  <div class="container">
    <h2 class="section-title">NemoClaw Architecture</h2>
    <p class="section-subtitle">Plugin + Blueprint composition &mdash; minimal CLI extension, independently versioned sandbox orchestration</p>

    <div class="arch-diagram-container">
      [SVG DIAGRAM — see below]
    </div>

    <div class="arch-legend">
      <div class="arch-legend-item"><span class="arch-legend-dot" style="background:#22c55e;"></span> Plugin (TypeScript)</div>
      <div class="arch-legend-item"><span class="arch-legend-dot" style="background:#2563eb;"></span> Blueprint (Python)</div>
      <div class="arch-legend-item"><span class="arch-legend-dot" style="background:#7c3aed;"></span> OpenShell Sandbox</div>
    </div>
  </div>
</section>
```

**Part C — The SVG diagram** must depict these relationships accurately per NemoClaw official docs:

The diagram should show a LEFT-TO-RIGHT flow with these components:

1. **OpenClaw CLI** (leftmost) — gray rounded rectangle, labeled "OpenClaw CLI"
2. **Arrow** from OpenClaw CLI to Plugin
3. **Plugin** (green-bordered box) — labeled "NemoClaw Plugin", subtitle "TypeScript package", with three action labels stacked: "resolve", "verify", "execute"
4. **Arrow** from Plugin to Blueprint labeled "subprocess"
5. **Blueprint** (blue-bordered box) — labeled "NemoClaw Blueprint", subtitle "Versioned Python artifact", with action labels: "plan sandbox", "apply policy", "configure inference"
6. **Arrow** from Blueprint to OpenShell
7. **OpenShell Sandbox** (purple-bordered box) — labeled "OpenShell Sandbox", subtitle "ghcr.io/nvidia/openshell-community", with a container icon or simple representation

The SVG should:
- Use viewBox="0 0 880 280" for a wide landscape layout
- Include role="img" and aria-label="NemoClaw Plugin+Blueprint architecture diagram showing the Plugin resolving and executing the Blueprint as a subprocess to create OpenShell sandboxes"
- Use the site's color palette: green (#22c55e) for Plugin, blue (#2563eb) for Blueprint, purple (#7c3aed) for OpenShell
- Use the same font styling patterns as the Blind Gates SVG (font-size in numeric values, fill for text color, text-anchor)
- Include arrow marker definitions in `<defs>`
- Arrows should use marker-end for direction

Keep the SVG clean and readable. Boxes should be ~160-180px wide, ~180px tall, with 12px rounded corners. Use subtle fill backgrounds (#f0fdf4 for green/plugin, #eff6ff for blue/blueprint, #faf5ff for purple/sandbox) matching the existing badge color scheme.
  </action>
  <verify>
    <automated>grep -q 'id="nemoclaw-architecture"' evidence-gate.github.io/index.html && grep -q 'href="#nemoclaw-architecture"' evidence-gate.github.io/index.html && grep -q 'viewBox' evidence-gate.github.io/index.html | head -1 && echo "PASS: Section, nav link, and SVG present" || echo "Checking individual elements..." && grep -c 'nemoclaw-architecture' evidence-gate.github.io/index.html</automated>
  </verify>
  <done>
    1. Nav bar contains an "Architecture" link pointing to #nemoclaw-architecture
    2. A new section with id="nemoclaw-architecture" exists between AI Governance and Ecosystem
    3. Section contains an inline SVG with Plugin (green), Blueprint (blue), and OpenShell Sandbox (purple) boxes
    4. SVG shows left-to-right flow: OpenClaw CLI -> Plugin (resolve/verify/execute) -> Blueprint (plan/apply/configure) -> OpenShell Sandbox
    5. SVG has role="img" and aria-label for accessibility
    6. Legend below diagram identifies the three color-coded components
  </done>
</task>

</tasks>

<verification>
1. Open evidence-gate.github.io/index.html in a browser
2. Verify the nav bar shows "Architecture" between "AI Governance" and "Ecosystem"
3. Click "Architecture" link — page scrolls to the new section
4. The section shows "NemoClaw Architecture" title with subtitle
5. An SVG diagram displays three main boxes (Plugin, Blueprint, OpenShell) with arrows showing the flow
6. A color legend appears below the diagram
7. The section has gray background (section-alt) and fits visually between AI Governance and Ecosystem
8. On mobile viewport (< 768px), the diagram scales down and legend stacks vertically
</verification>

<success_criteria>
- New section visible between AI Governance and Ecosystem with correct id
- SVG architecture diagram accurately depicts Plugin+Blueprint+OpenShell composition
- Nav link works and scrolls to the section
- Site does not break — all existing sections render correctly
- Responsive: diagram scales on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/05-architecture-diagrams-and-technical-foundation/05-01-SUMMARY.md`
</output>
